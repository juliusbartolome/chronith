# Chronith v0.1 Continuation Plan (Tasks 7–11)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete Chronith v0.1 — fix the broken API build, add full functional/performance/load tests, and containerize.

**Architecture:** .NET 10 Clean Architecture (Domain → Application → Infrastructure → API). FastEndpoints 8 for HTTP, MediatR for CQRS, EF Core + Postgres for persistence, multi-tenant via JWT claims.

**Tech Stack:** .NET 10, FastEndpoints 8, MediatR, EF Core, Postgres, xunit, FluentAssertions, Testcontainers, BenchmarkDotNet, k6, Docker.

---

## Auth Matrix (derived from endpoint Roles() declarations)

| Endpoint                                     | TenantAdmin | TenantStaff | Customer | TenantPaymentService | Unauthenticated |
| -------------------------------------------- | ----------- | ----------- | -------- | -------------------- | --------------- |
| `GET /booking-types`                         | 200         | 200         | 200      | 403                  | 401             |
| `POST /booking-types`                        | 201         | 403         | 403      | 403                  | 401             |
| `GET /booking-types/{slug}`                  | 200         | 200         | 200      | 403                  | 401             |
| `PUT /booking-types/{slug}`                  | 200         | 403         | 403      | 403                  | 401             |
| `DELETE /booking-types/{slug}`               | 204         | 403         | 403      | 403                  | 401             |
| `GET /booking-types/{slug}/availability`     | 200         | 200         | 200      | 403                  | 401             |
| `GET /booking-types/{slug}/bookings`         | 200         | 200         | 403      | 403                  | 401             |
| `POST /booking-types/{slug}/bookings`        | 201         | 201         | 201      | 403                  | 401             |
| `GET /bookings/{id}`                         | 200         | 200         | 200      | 403                  | 401             |
| `POST /bookings/{id}/cancel`                 | 200         | 200         | 200      | 403                  | 401             |
| `POST /bookings/{id}/confirm`                | 200         | 200         | 403      | 403                  | 401             |
| `POST /bookings/{id}/pay`                    | 200         | 200         | 403      | 200                  | 401             |
| `GET /booking-types/{slug}/webhooks`         | 200         | 403         | 403      | 403                  | 401             |
| `POST /booking-types/{slug}/webhooks`        | 201         | 403         | 403      | 403                  | 401             |
| `DELETE /booking-types/{slug}/webhooks/{id}` | 204         | 403         | 403      | 403                  | 401             |
| `GET /tenant`                                | 200         | 403         | 403      | 403                  | 401             |
| `POST /webhooks/payment`                     | 403         | 403         | 403      | 200                  | 401             |

---

## Task 7 — Fix FastEndpoints 8 Build Errors

**Problem:** FastEndpoints 8 moved response helpers off the endpoint base class onto a `Send` property.
All instance-method calls (`SendOkAsync`, `SendCreatedAtAsync`, `SendAsync`) must become `Send.OkAsync`, `Send.CreatedAtAsync`, `Send.Async`.

**Files to modify** (12 files — 2 already correct and untouched):

| File                                                                   | Old call                                      | New call                                       |
| ---------------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------- |
| `src/Chronith.API/Endpoints/Availability/GetAvailabilityEndpoint.cs`   | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Bookings/CancelBookingEndpoint.cs`         | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Bookings/ConfirmBookingEndpoint.cs`        | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Bookings/CreateBookingEndpoint.cs`         | `SendCreatedAtAsync<GetBookingEndpoint>(`     | `Send.CreatedAtAsync<GetBookingEndpoint>(`     |
| `src/Chronith.API/Endpoints/Bookings/GetBookingEndpoint.cs`            | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Bookings/ListBookingsEndpoint.cs`          | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Bookings/PayBookingEndpoint.cs`            | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/BookingTypes/CreateBookingTypeEndpoint.cs` | `SendCreatedAtAsync<GetBookingTypeEndpoint>(` | `Send.CreatedAtAsync<GetBookingTypeEndpoint>(` |
| `src/Chronith.API/Endpoints/BookingTypes/GetBookingTypeEndpoint.cs`    | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/BookingTypes/ListBookingTypesEndpoint.cs`  | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/BookingTypes/UpdateBookingTypeEndpoint.cs` | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Tenant/GetTenantEndpoint.cs`               | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Payments/PaymentWebhookEndpoint.cs`        | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |
| `src/Chronith.API/Endpoints/Webhooks/CreateWebhookEndpoint.cs`         | `SendAsync(result, 201, ct)`                  | `Send.Async(result, 201, ct)`                  |
| `src/Chronith.API/Endpoints/Webhooks/ListWebhooksEndpoint.cs`          | `SendOkAsync(result, ct)`                     | `Send.OkAsync(result, ct)`                     |

**Step 1: Apply all 15 renames**

Edit each file above with the corresponding replacement.

**Step 2: Verify build**

Run: `dotnet build Chronith.slnx`
Expected: `Build succeeded. 0 Error(s)`

**Step 3: Verify unit tests still pass**

Run: `dotnet test tests/Chronith.Tests.Unit/Chronith.Tests.Unit.csproj`
Expected: `Passed! - Failed: 0, Passed: 56`

**Step 4: Commit**

```
git add src/Chronith.API/Endpoints/
git commit -m "fix(api): update FastEndpoints 8 Send method calls (SendOkAsync → Send.OkAsync)"
```

---

## Task 8 — Functional Tests (full auth matrix)

**Goal:** End-to-end HTTP tests via `WebApplicationFactory<Program>` + Testcontainers Postgres, covering full auth matrix and booking lifecycle.

**Step 1: Add FastEndpoints.Security package**

Run: `dotnet add tests/Chronith.Tests.Functional/Chronith.Tests.Functional.csproj package FastEndpoints.Security`

**Step 2: Create `Fixtures/TestConstants.cs`**

Create: `tests/Chronith.Tests.Functional/Fixtures/TestConstants.cs`

```csharp
namespace Chronith.Tests.Functional.Fixtures;

public static class TestConstants
{
    public const string JwtSigningKey = "functional-test-signing-key-at-least-32-chars!!";
    public static readonly Guid TenantId = Guid.Parse("00000000-0000-0000-0000-000000000001");
    public const string AdminUserId      = "user-admin-1";
    public const string StaffUserId      = "user-staff-1";
    public const string CustomerUserId   = "user-customer-1";
    public const string PaymentSvcUserId = "user-paymentsvc-1";
}
```

**Step 3: Create `Fixtures/TestJwtFactory.cs`**

Create: `tests/Chronith.Tests.Functional/Fixtures/TestJwtFactory.cs`

```csharp
using FastEndpoints.Security;

namespace Chronith.Tests.Functional.Fixtures;

public static class TestJwtFactory
{
    public static string CreateToken(string role, string userId, Guid? tenantId = null)
    {
        var tid = (tenantId ?? TestConstants.TenantId).ToString();
        return JwtBearer.CreateToken(o =>
        {
            o.SigningKey = TestConstants.JwtSigningKey;
            o.ExpireAt = DateTime.UtcNow.AddHours(1);
            o.User.Claims.Add(("tenant_id", tid));
            o.User.Claims.Add(("sub", userId));
            o.User.Roles.Add(role);
        });
    }
}
```

**Step 4: Create `Fixtures/FunctionalTestFixture.cs`**

Create: `tests/Chronith.Tests.Functional/Fixtures/FunctionalTestFixture.cs`

```csharp
using Chronith.Infrastructure.Persistence;
using Microsoft.AspNetCore.Hosting;
using Microsoft.AspNetCore.Mvc.Testing;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using Testcontainers.PostgreSql;

namespace Chronith.Tests.Functional.Fixtures;

public sealed class FunctionalTestFixture : IAsyncLifetime
{
    private readonly PostgreSqlContainer _postgres = new PostgreSqlBuilder()
        .WithImage("postgres:17-alpine")
        .WithDatabase("chronith_functional")
        .WithUsername("test")
        .WithPassword("test")
        .Build();

    public WebApplicationFactory<Program> Factory { get; private set; } = null!;

    public async Task InitializeAsync()
    {
        await _postgres.StartAsync();

        Factory = new WebApplicationFactory<Program>()
            .WithWebHostBuilder(builder =>
            {
                builder.UseSetting("Database:Provider", "PostgreSQL");
                builder.UseSetting("Database:ConnectionString", _postgres.GetConnectionString());
                builder.UseSetting("Jwt:SigningKey", TestConstants.JwtSigningKey);
            });

        // Run migrations
        using var scope = Factory.Services.CreateScope();
        var db = scope.ServiceProvider.GetRequiredService<ChronithDbContext>();
        await db.Database.MigrateAsync();
    }

    public async Task DisposeAsync()
    {
        await Factory.DisposeAsync();
        await _postgres.DisposeAsync();
    }

    public HttpClient CreateClient(string role, string? userId = null) =>
        CreateClientWithToken(TestJwtFactory.CreateToken(role, userId ?? RoleToUserId(role)));

    public HttpClient CreateAnonymousClient() =>
        Factory.CreateClient();

    private HttpClient CreateClientWithToken(string token)
    {
        var client = Factory.CreateClient();
        client.DefaultRequestHeaders.Authorization =
            new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", token);
        return client;
    }

    private static string RoleToUserId(string role) => role switch
    {
        "TenantAdmin"          => TestConstants.AdminUserId,
        "TenantStaff"          => TestConstants.StaffUserId,
        "Customer"             => TestConstants.CustomerUserId,
        "TenantPaymentService" => TestConstants.PaymentSvcUserId,
        _                      => "user-unknown"
    };
}

[CollectionDefinition("Functional")]
public class FunctionalCollection : ICollectionFixture<FunctionalTestFixture> { }
```

**Step 5: Create `Helpers/SeedData.cs`**

Create: `tests/Chronith.Tests.Functional/Helpers/SeedData.cs`

```csharp
using Chronith.Application.Interfaces;
using Chronith.Domain.Enums;
using Chronith.Infrastructure.Persistence;
using Chronith.Infrastructure.Persistence.Entities;
using Chronith.Tests.Functional.Fixtures;
using Microsoft.Extensions.DependencyInjection;

namespace Chronith.Tests.Functional.Helpers;

public static class SeedData
{
    private sealed class StubTenantContext : ITenantContext
    {
        public Guid TenantId { get; }
        public string UserId => TestConstants.AdminUserId;
        public string Role => "TenantAdmin";
        public StubTenantContext(Guid tenantId) => TenantId = tenantId;
    }

    public static ChronithDbContext CreateDbContext(
        WebApplicationFactory<Program> factory,
        Guid? tenantId = null)
    {
        var scope = factory.Services.CreateScope();
        // Replace ITenantContext with stub so EF global filters work
        var options = scope.ServiceProvider
            .GetRequiredService<Microsoft.EntityFrameworkCore.DbContextOptions<ChronithDbContext>>();
        return new ChronithDbContext(options, new StubTenantContext(tenantId ?? TestConstants.TenantId));
    }

    public static async Task<Guid> SeedTenantAsync(ChronithDbContext db, Guid? id = null, string slug = "test-tenant")
    {
        var tenantId = id ?? TestConstants.TenantId;
        // Idempotent — skip if already exists
        if (await db.Tenants.FindAsync(tenantId) is not null) return tenantId;
        db.Tenants.Add(new TenantEntity
        {
            Id = tenantId,
            Slug = slug,
            Name = "Test Tenant",
            TimeZoneId = "UTC",
            IsDeleted = false
        });
        await db.SaveChangesAsync();
        return tenantId;
    }

    public static async Task<Guid> SeedBookingTypeAsync(
        ChronithDbContext db,
        string slug = "test-type",
        int capacity = 5,
        int durationMinutes = 60)
    {
        var id = Guid.NewGuid();
        db.BookingTypes.Add(new BookingTypeEntity
        {
            Id = id,
            TenantId = TestConstants.TenantId,
            Slug = slug,
            Name = "Test Type",
            Kind = BookingKind.TimeSlot,
            Capacity = capacity,
            PaymentMode = PaymentMode.Manual,
            IsDeleted = false,
            DurationMinutes = durationMinutes,
            BufferBeforeMinutes = 0,
            BufferAfterMinutes = 0
        });
        await db.SaveChangesAsync();
        return id;
    }

    public static async Task<Guid> SeedBookingAsync(
        ChronithDbContext db,
        Guid bookingTypeId,
        DateTimeOffset start,
        DateTimeOffset end,
        BookingStatus status = BookingStatus.Pending,
        string customerId = "cust-seed-1")
    {
        var id = Guid.NewGuid();
        db.Bookings.Add(new BookingEntity
        {
            Id = id,
            TenantId = TestConstants.TenantId,
            BookingTypeId = bookingTypeId,
            Start = start,
            End = end,
            Status = status,
            CustomerId = customerId,
            CustomerEmail = $"{customerId}@example.com",
            IsDeleted = false
        });
        await db.SaveChangesAsync();
        return id;
    }
}
```

**Step 6: Create auth matrix test files**

Create the following files. Each uses `[Collection("Functional")]` and `IClassFixture` is handled by the collection fixture.

**`tests/Chronith.Tests.Functional/BookingTypes/BookingTypeAuthTests.cs`** — 25 test cases covering all 5 roles × 5 endpoints.

**`tests/Chronith.Tests.Functional/BookingTypes/BookingTypeEndpointsTests.cs`** — happy-path CRUD as TenantAdmin.

**`tests/Chronith.Tests.Functional/Availability/AvailabilityAuthTests.cs`** — 5 role cases for `GET /booking-types/{slug}/availability`.

**`tests/Chronith.Tests.Functional/Availability/AvailabilityEndpointsTests.cs`** — seeds booking type, asserts availability response shape.

**`tests/Chronith.Tests.Functional/Bookings/BookingAuthTests.cs`** — 30 cases for all 6 booking endpoints × 5 roles.

**`tests/Chronith.Tests.Functional/Bookings/BookingLifecycleTests.cs`** — create → confirm → pay → get, assert status transitions.

**`tests/Chronith.Tests.Functional/Webhooks/WebhookAuthTests.cs`** — 15 cases for 3 webhook endpoints × 5 roles.

**`tests/Chronith.Tests.Functional/Webhooks/WebhookEndpointsTests.cs`** — create → list → delete happy path.

**`tests/Chronith.Tests.Functional/Tenant/TenantAuthTests.cs`** — 5 cases for `GET /tenant`.

**`tests/Chronith.Tests.Functional/Tenant/TenantEndpointsTests.cs`** — assert tenant response body.

**`tests/Chronith.Tests.Functional/Payments/PaymentWebhookAuthTests.cs`** — 5 cases for `POST /webhooks/payment`.

**Step 7: Run functional tests**

Run: `dotnet test tests/Chronith.Tests.Functional/Chronith.Tests.Functional.csproj`
Expected: all green.

**Step 8: Commit**

```
git add tests/Chronith.Tests.Functional/
git commit -m "test(functional): full auth matrix + booking lifecycle functional tests"
```

---

## Task 9 — Performance Benchmarks

**Goal:** BenchmarkDotNet benchmarks for slot generation and conflict detection.

**Step 1: Add InternalsVisibleTo for Performance project**

Modify: `src/Chronith.Domain/Chronith.Domain.csproj`
Add: `<InternalsVisibleTo Include="Chronith.Tests.Performance" />`

**Step 2: Create `Program.cs`**

Create: `tests/Chronith.Tests.Performance/Program.cs`

```csharp
using BenchmarkDotNet.Running;

BenchmarkSwitcher.FromAssembly(typeof(Program).Assembly).Run(args);
```

**Step 3: Create `Benchmarks/SlotGenerationBenchmarks.cs`**

Create: `tests/Chronith.Tests.Performance/Benchmarks/SlotGenerationBenchmarks.cs`

Scenarios (using `[Params]`):

- TimeSlot, 1-week range, 0 existing bookings
- TimeSlot, 1-week range, 500 existing bookings
- Calendar, 1-week range
- Calendar, 30-day range

Use `[GlobalSetup]` to build the `BookingType` and existing bookings list inline (no reference to test project builders).

**Step 4: Create `Benchmarks/ConflictRangeBenchmarks.cs`**

Create: `tests/Chronith.Tests.Performance/Benchmarks/ConflictRangeBenchmarks.cs`

Scenarios:

- 10k bookings, overlap found on first check
- 10k bookings, no overlap (worst case scan)

**Step 5: Verify dry run**

Run: `dotnet run --project tests/Chronith.Tests.Performance -- --job dry`
Expected: exits 0, prints benchmark summary without actually running.

**Step 6: Commit**

```
git add src/Chronith.Domain/Chronith.Domain.csproj tests/Chronith.Tests.Performance/
git commit -m "test(performance): BenchmarkDotNet slot generation and conflict range benchmarks"
```

---

## Task 10 — k6 Load Tests

**Goal:** Load test scripts ready to run against a live instance at `BASE_URL` (default `http://localhost:5000`).

**Step 1: Create `scripts/helpers.js`**

Create: `tests/Chronith.Tests.Load/scripts/helpers.js`

Exports:

- `baseUrl()` — reads `__ENV.BASE_URL` or `http://localhost:5000`
- `authHeader(role)` — returns `{ Authorization: "Bearer <jwt>" }` using HMAC-SHA256 signed JWT (k6 `crypto` module)
- JWT payload includes `tenant_id`, `sub`, `role` claims

**Step 2: Create `scripts/availability.js`**

Create: `tests/Chronith.Tests.Load/scripts/availability.js`

- 100 VUs, 60s duration
- Threshold: `http_req_duration['p(95)'] < 100`
- Hits `GET /booking-types/test-type/availability?from=...&to=...` with Customer token

**Step 3: Create `scripts/create-booking.js`**

Create: `tests/Chronith.Tests.Load/scripts/create-booking.js`

- 50 VUs, 30s duration
- Threshold: `http_req_duration['p(95)'] < 200`
- POSTs unique bookings (email = `k6-${__VU}-${__ITER}@example.com`)

**Step 4: Create `scripts/concurrent-booking.js`**

Create: `tests/Chronith.Tests.Load/scripts/concurrent-booking.js`

- 50 VUs, same slot, capacity=1
- Asserts: exactly 1 success (201), rest 409 or 400
- Checks `booking_conflicts` custom counter

**Step 5: Create `scripts/booking-lifecycle.js`**

Create: `tests/Chronith.Tests.Load/scripts/booking-lifecycle.js`

- 20 VUs, 30s duration
- Threshold: `http_req_duration['p(95)'] < 500`
- Full flow: create (Customer) → confirm (Staff) → get (Admin)

**Step 6: Create `README.md`**

Create: `tests/Chronith.Tests.Load/README.md`

- Prerequisites: k6 installed, running instance, seed data
- Run commands for each script
- Expected thresholds
- Note: concurrent-booking requires pre-seeded booking type with capacity=1

**Step 7: Verify static analysis**

Run: `k6 inspect tests/Chronith.Tests.Load/scripts/availability.js`
Expected: prints script metadata, exits 0 (no server needed).

**Step 8: Commit**

```
git add tests/Chronith.Tests.Load/
git commit -m "test(load): k6 scripts for availability latency, concurrent booking, lifecycle"
```

---

## Task 11 — Containerization + TEST_PLAN.md

**Goal:** `docker compose up` → API starts, health checks pass at `/health/live` and `/health/ready`.

**Step 1: Create `.dockerignore`**

Create: `.dockerignore`

```
**/bin/
**/obj/
.git/
tests/
docs/
*.md
*.user
.vs/
```

**Step 2: Create `Dockerfile`**

Create: `Dockerfile` — 3-stage build:

1. `restore` — `mcr.microsoft.com/dotnet/sdk:10.0`, WORKDIR `/src`, copy `*.slnx` + all `*.csproj`, run `dotnet restore Chronith.slnx`
2. `build` — copy source, run `dotnet publish src/Chronith.API/Chronith.API.csproj -c Release -o /app/out --no-restore`
3. `runtime` — `mcr.microsoft.com/dotnet/aspnet:10.0`, non-root user `app` (uid 1000), WORKDIR `/app`, COPY from build, EXPOSE 8080, HEALTHCHECK `curl -f http://localhost:8080/health/live || exit 1`, ENTRYPOINT `["dotnet", "Chronith.API.dll"]`

**Step 3: Create `docker-compose.yml`**

Create: `docker-compose.yml`

Services:

- `postgres` — `postgres:17-alpine`, env `POSTGRES_DB=chronith POSTGRES_USER=chronith POSTGRES_PASSWORD=chronith`, healthcheck `pg_isready -U chronith`
- `chronith-api` — build `.`, depends_on postgres `service_healthy`, env `Database__Provider=PostgreSQL Database__ConnectionString=... Jwt__SigningKey=change-me-in-production-at-least-32-chars ASPNETCORE_URLS=http://+:8080`

**Step 4: Create `docker-compose.override.yml`**

Create: `docker-compose.override.yml`

```yaml
services:
  chronith-api:
    ports:
      - "5000:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
```

**Step 5: Create `TEST_PLAN.md`**

Create: `TEST_PLAN.md`

Sections:

- Scope & goals
- 5 test layers: Unit / Integration / Functional / Performance / Load
- Run commands for each layer
- Coverage areas per layer
- Performance targets (availability p95<100ms, lifecycle p95<500ms)
- Full auth matrix table
- Known gaps / future work

**Step 6: Verify Docker build**

Run: `docker compose build`
Expected: exits 0, image built successfully.

**Step 7: Commit**

```
git add Dockerfile docker-compose.yml docker-compose.override.yml .dockerignore TEST_PLAN.md
git commit -m "feat(containers): multi-stage Dockerfile, docker-compose, health checks, TEST_PLAN.md"
```
