# Chronith v0.1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build Chronith v0.1 — a multi-tenant booking engine supporting TimeSlot and Calendar booking types, with strict TDD, EF Core + PostgreSQL, FastEndpoints, MediatR, and full test coverage across unit/integration/functional/performance/load layers.

**Architecture:** Clean Architecture (Domain → Application → Infrastructure → API). Two domain booking type classes (`TimeSlotBookingType`, `CalendarBookingType`) backed by a single EF entity with discriminator. Conflict detection runs as SQL COUNT query. Availability generation uses one DB round-trip + in-memory slot subtraction. Optimistic concurrency via EF RowVersion.

**Tech Stack:** .NET 10, EF Core 10, Npgsql, MediatR, FastEndpoints, FluentValidation, xUnit, FluentAssertions, Testcontainers, BenchmarkDotNet, k6

---

## Global Rules (apply to every task)

- Always run `dotnet build` before committing — no build failures allowed
- Always run relevant tests before committing — no test failures allowed
- Follow strict TDD: write failing test → implement → pass → refactor → commit
- Manual mapping only (static extension methods) — no AutoMapper, no Mapster
- AsNoTracking on all reads
- No provider-specific SQL
- Commit after every task with a clear conventional commit message
- `.gitignore` must be present from Task 1

---

## Task 1: Solution Scaffold + .gitignore

**Goal:** Create the solution, all projects, project references, NuGet packages, and .gitignore. Everything compiles. Nothing more.

**Files to create:**

- `Chronith.sln`
- `src/Chronith.Domain/Chronith.Domain.csproj`
- `src/Chronith.Application/Chronith.Application.csproj`
- `src/Chronith.Infrastructure/Chronith.Infrastructure.csproj`
- `src/Chronith.API/Chronith.API.csproj`
- `tests/Chronith.Tests.Unit/Chronith.Tests.Unit.csproj`
- `tests/Chronith.Tests.Integration/Chronith.Tests.Integration.csproj`
- `tests/Chronith.Tests.Functional/Chronith.Tests.Functional.csproj`
- `tests/Chronith.Tests.Performance/Chronith.Tests.Performance.csproj`
- `tests/Chronith.Tests.Load/` (k6 scripts — no .csproj)
- `.gitignore`
- `global.json` (pin .NET 10)

**Project references:**

```
Application  → Domain
Infrastructure → Application
API → Application
API → Infrastructure
Tests.Unit → Domain, Application
Tests.Integration → Infrastructure, Application
Tests.Functional → API
Tests.Performance → Domain, Application
```

**NuGet packages:**

| Project           | Packages                                                                                                                    |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Domain            | (none — pure C#)                                                                                                            |
| Application       | MediatR, FluentValidation, MediatR.Extensions.Microsoft.DependencyInjection                                                 |
| Infrastructure    | Microsoft.EntityFrameworkCore, Npgsql.EntityFrameworkCore.PostgreSQL, Microsoft.Extensions.DependencyInjection.Abstractions |
| API               | FastEndpoints, FastEndpoints.Security, Microsoft.AspNetCore.Diagnostics.HealthChecks, Serilog.AspNetCore                    |
| Tests.Unit        | xUnit, xunit.runner.visualstudio, FluentAssertions, Microsoft.NET.Test.Sdk                                                  |
| Tests.Integration | xUnit, FluentAssertions, Testcontainers.PostgreSql, Microsoft.EntityFrameworkCore.Design                                    |
| Tests.Functional  | xUnit, FluentAssertions, Microsoft.AspNetCore.Mvc.Testing, Testcontainers.PostgreSql                                        |
| Tests.Performance | BenchmarkDotNet                                                                                                             |

**Step 1:** Create the solution and all projects using `dotnet new` commands.

```bash
dotnet new sln -n Chronith
dotnet new classlib -n Chronith.Domain -o src/Chronith.Domain --framework net10.0
dotnet new classlib -n Chronith.Application -o src/Chronith.Application --framework net10.0
dotnet new classlib -n Chronith.Infrastructure -o src/Chronith.Infrastructure --framework net10.0
dotnet new webapi -n Chronith.API -o src/Chronith.API --framework net10.0 --use-minimal-apis
dotnet new xunit -n Chronith.Tests.Unit -o tests/Chronith.Tests.Unit --framework net10.0
dotnet new xunit -n Chronith.Tests.Integration -o tests/Chronith.Tests.Integration --framework net10.0
dotnet new xunit -n Chronith.Tests.Functional -o tests/Chronith.Tests.Functional --framework net10.0
dotnet new classlib -n Chronith.Tests.Performance -o tests/Chronith.Tests.Performance --framework net10.0
mkdir -p tests/Chronith.Tests.Load/scripts
```

**Step 2:** Add all projects to solution.

**Step 3:** Add project references per the matrix above.

**Step 4:** Add NuGet packages per the matrix above.

**Step 5:** Delete boilerplate files (`Class1.cs`, `WeatherForecast.cs`, etc.).

**Step 6:** Create `.gitignore` (standard .NET gitignore from `dotnet new gitignore`).

**Step 7:** Create `global.json`:

```json
{
  "sdk": {
    "version": "10.0.0",
    "rollForward": "latestMinor"
  }
}
```

**Step 8:** Run `dotnet build Chronith.sln` — must succeed with 0 errors, 0 warnings.

**Step 9:** Commit.

```bash
git init
git add .
git commit -m "chore: scaffold solution with all projects, references, and packages"
```

---

## Task 2: Domain Layer — Entities, Enums, Exceptions

**Goal:** All domain types — entities, value objects, enums, domain exceptions. Pure C#, no EF attributes, no external dependencies.

**Files to create:**

- `src/Chronith.Domain/Enums/BookingKind.cs`
- `src/Chronith.Domain/Enums/BookingStatus.cs`
- `src/Chronith.Domain/Enums/PaymentMode.cs`
- `src/Chronith.Domain/Exceptions/DomainException.cs`
- `src/Chronith.Domain/Exceptions/SlotConflictException.cs`
- `src/Chronith.Domain/Exceptions/NotFoundException.cs`
- `src/Chronith.Domain/Exceptions/InvalidStateTransitionException.cs`
- `src/Chronith.Domain/Exceptions/SlotNotInWindowException.cs`
- `src/Chronith.Domain/Models/TenantTimeZone.cs`
- `src/Chronith.Domain/Models/Tenant.cs`
- `src/Chronith.Domain/Models/BookingType.cs` # abstract base
- `src/Chronith.Domain/Models/TimeSlotBookingType.cs`
- `src/Chronith.Domain/Models/CalendarBookingType.cs`
- `src/Chronith.Domain/Models/TimeSlotWindow.cs`
- `src/Chronith.Domain/Models/Booking.cs`
- `src/Chronith.Domain/Models/BookingStatusChange.cs`
- `src/Chronith.Domain/Models/Webhook.cs`

**Enums:**

```csharp
// BookingKind.cs
public enum BookingKind { TimeSlot, Calendar }

// BookingStatus.cs
public enum BookingStatus { PendingPayment, PendingVerification, Confirmed, Cancelled }

// PaymentMode.cs
public enum PaymentMode { Manual, Automatic }
```

**Exceptions:**

```csharp
public abstract class DomainException : Exception
{
    protected DomainException(string message) : base(message) { }
}

public sealed class SlotConflictException : DomainException
{
    public SlotConflictException() : base("The requested slot is not available.") { }
}

public sealed class NotFoundException : DomainException
{
    public NotFoundException(string resource, object id)
        : base($"{resource} with id '{id}' was not found.") { }
}

public sealed class InvalidStateTransitionException : DomainException
{
    public InvalidStateTransitionException(BookingStatus from, string action)
        : base($"Cannot perform '{action}' on a booking in '{from}' status.") { }
}

public sealed class SlotNotInWindowException : DomainException
{
    public SlotNotInWindowException(DateTimeOffset requested)
        : base($"Requested start '{requested:O}' does not fall within any availability window.") { }
}
```

**TenantTimeZone value object:**

```csharp
public sealed class TenantTimeZone
{
    public string IanaId { get; }
    private readonly TimeZoneInfo _tz;

    public TenantTimeZone(string ianaId)
    {
        IanaId = ianaId;
        _tz = TimeZoneInfo.FindSystemTimeZoneById(ianaId);
    }

    public DateTimeOffset ToUtc(DateOnly date, TimeOnly time)
        => TimeZoneInfo.ConvertTimeToUtc(
            new DateTime(date.Year, date.Month, date.Day, time.Hour, time.Minute, 0, DateTimeKind.Unspecified),
            _tz);

    public DateOnly ToLocalDate(DateTimeOffset utc)
        => DateOnly.FromDateTime(TimeZoneInfo.ConvertTimeFromUtc(utc.UtcDateTime, _tz));
}
```

**Abstract BookingType:**

```csharp
public abstract class BookingType
{
    public Guid Id { get; protected set; }
    public Guid TenantId { get; protected set; }
    public string Slug { get; protected set; } = string.Empty;
    public string Name { get; protected set; } = string.Empty;
    public int Capacity { get; protected set; }
    public PaymentMode PaymentMode { get; protected set; }
    public string? PaymentProvider { get; protected set; }
    public bool IsDeleted { get; protected set; }

    // Returns the UTC start and end for a booking given a requested start in UTC
    public abstract (DateTimeOffset Start, DateTimeOffset End) ResolveSlot(
        DateTimeOffset requestedStart, TenantTimeZone tz);

    // Returns the effective conflict range (adjusted for buffers)
    public abstract (DateTimeOffset EffectiveStart, DateTimeOffset EffectiveEnd)
        GetEffectiveRange(DateTimeOffset start, DateTimeOffset end);
}
```

**TimeSlotBookingType:**

```csharp
public sealed class TimeSlotBookingType : BookingType
{
    public int DurationMinutes { get; private set; }
    public int BufferBeforeMinutes { get; private set; }
    public int BufferAfterMinutes { get; private set; }
    public IReadOnlyList<TimeSlotWindow> AvailabilityWindows { get; private set; }
        = Array.Empty<TimeSlotWindow>();

    // For Infrastructure hydration
    internal TimeSlotBookingType() { }

    public override (DateTimeOffset Start, DateTimeOffset End) ResolveSlot(
        DateTimeOffset requestedStart, TenantTimeZone tz)
    {
        var end = requestedStart.AddMinutes(DurationMinutes);
        var localDate = tz.ToLocalDate(requestedStart);
        var dow = localDate.DayOfWeek;
        var localTime = TimeOnly.FromTimeSpan(
            TimeZoneInfo.ConvertTimeFromUtc(requestedStart.UtcDateTime,
                TimeZoneInfo.FindSystemTimeZoneById(tz.IanaId)).TimeOfDay);

        var window = AvailabilityWindows.FirstOrDefault(w =>
            w.DayOfWeek == dow &&
            localTime >= w.StartTime &&
            localTime.AddMinutes(DurationMinutes) <= w.EndTime);

        if (window is null)
            throw new SlotNotInWindowException(requestedStart);

        return (requestedStart, end);
    }

    public override (DateTimeOffset EffectiveStart, DateTimeOffset EffectiveEnd)
        GetEffectiveRange(DateTimeOffset start, DateTimeOffset end)
        => (start.AddMinutes(-BufferBeforeMinutes), end.AddMinutes(BufferAfterMinutes));
}
```

**CalendarBookingType:**

```csharp
public sealed class CalendarBookingType : BookingType
{
    public IReadOnlyList<DayOfWeek> AvailableDays { get; private set; }
        = Array.Empty<DayOfWeek>();

    internal CalendarBookingType() { }

    public override (DateTimeOffset Start, DateTimeOffset End) ResolveSlot(
        DateTimeOffset requestedStart, TenantTimeZone tz)
    {
        var localDate = tz.ToLocalDate(requestedStart);
        if (!AvailableDays.Contains(localDate.DayOfWeek))
            throw new SlotNotInWindowException(requestedStart);

        var dayStart = tz.ToUtc(localDate, TimeOnly.MinValue);
        var dayEnd   = tz.ToUtc(localDate.AddDays(1), TimeOnly.MinValue);
        return (dayStart, dayEnd);
    }

    public override (DateTimeOffset EffectiveStart, DateTimeOffset EffectiveEnd)
        GetEffectiveRange(DateTimeOffset start, DateTimeOffset end)
        => (start, end); // No buffers for calendar bookings
}
```

**TimeSlotWindow:**

```csharp
public sealed class TimeSlotWindow
{
    public DayOfWeek DayOfWeek { get; }
    public TimeOnly StartTime { get; }
    public TimeOnly EndTime { get; }

    public TimeSlotWindow(DayOfWeek dayOfWeek, TimeOnly startTime, TimeOnly endTime)
    {
        if (endTime <= startTime)
            throw new ArgumentException("EndTime must be after StartTime.");
        DayOfWeek = dayOfWeek;
        StartTime = startTime;
        EndTime = endTime;
    }
}
```

**Booking:**

```csharp
public sealed class Booking
{
    public Guid Id { get; private set; }
    public Guid TenantId { get; private set; }
    public Guid BookingTypeId { get; private set; }
    public DateTimeOffset Start { get; private set; }
    public DateTimeOffset End { get; private set; }
    public BookingStatus Status { get; private set; }
    public string CustomerId { get; private set; } = string.Empty;
    public string CustomerEmail { get; private set; } = string.Empty;
    public string? PaymentReference { get; private set; }
    public bool IsDeleted { get; private set; }
    public uint RowVersion { get; private set; }

    private readonly List<BookingStatusChange> _statusChanges = new();
    public IReadOnlyList<BookingStatusChange> StatusChanges => _statusChanges.AsReadOnly();

    internal Booking() { }

    public static Booking Create(
        Guid tenantId, Guid bookingTypeId,
        DateTimeOffset start, DateTimeOffset end,
        string customerId, string customerEmail,
        string? paymentReference = null)
    {
        return new Booking
        {
            Id = Guid.NewGuid(),
            TenantId = tenantId,
            BookingTypeId = bookingTypeId,
            Start = start,
            End = end,
            Status = BookingStatus.PendingPayment,
            CustomerId = customerId,
            CustomerEmail = customerEmail,
            PaymentReference = paymentReference
        };
    }

    public void Pay(string changedById, string changedByRole)
    {
        if (Status != BookingStatus.PendingPayment)
            throw new InvalidStateTransitionException(Status, "pay");
        Transition(BookingStatus.PendingVerification, changedById, changedByRole);
    }

    public void Confirm(string changedById, string changedByRole)
    {
        if (Status != BookingStatus.PendingVerification)
            throw new InvalidStateTransitionException(Status, "confirm");
        Transition(BookingStatus.Confirmed, changedById, changedByRole);
    }

    public void Cancel(string changedById, string changedByRole)
    {
        if (Status == BookingStatus.Cancelled)
            throw new InvalidStateTransitionException(Status, "cancel");
        Transition(BookingStatus.Cancelled, changedById, changedByRole);
    }

    private void Transition(BookingStatus to, string changedById, string changedByRole)
    {
        var change = new BookingStatusChange(Id, Status, to, changedById, changedByRole);
        _statusChanges.Add(change);
        Status = to;
    }
}
```

**BookingStatusChange:**

```csharp
public sealed class BookingStatusChange
{
    public Guid Id { get; }
    public Guid BookingId { get; }
    public BookingStatus FromStatus { get; }
    public BookingStatus ToStatus { get; }
    public string ChangedById { get; }
    public string ChangedByRole { get; }
    public DateTimeOffset ChangedAt { get; }

    public BookingStatusChange(
        Guid bookingId, BookingStatus from, BookingStatus to,
        string changedById, string changedByRole)
    {
        Id = Guid.NewGuid();
        BookingId = bookingId;
        FromStatus = from;
        ToStatus = to;
        ChangedById = changedById;
        ChangedByRole = changedByRole;
        ChangedAt = DateTimeOffset.UtcNow;
    }
}
```

**Step 1:** Write failing unit tests for all domain rules (Task 3 below is the full unit test suite — write a minimal set here to drive the domain implementations).

**Step 2:** Implement all domain types above.

**Step 3:** Run `dotnet build` — 0 errors.

**Step 4:** Run `dotnet test tests/Chronith.Tests.Unit` — all pass.

**Step 5:** Commit.

```bash
git add .
git commit -m "feat(domain): add all domain entities, enums, exceptions, and value objects"
```

---

## Task 3: Domain Unit Tests (Full Suite)

**Goal:** Full unit test coverage of all domain rules. Tests run with zero dependencies — no DB, no HTTP.

**Files to create:**

- `tests/Chronith.Tests.Unit/Domain/BookingStateMachineTests.cs`
- `tests/Chronith.Tests.Unit/Domain/TimeSlotConflictRangeTests.cs`
- `tests/Chronith.Tests.Unit/Domain/CalendarConflictRangeTests.cs`
- `tests/Chronith.Tests.Unit/Domain/TimeSlotGenerationTests.cs`
- `tests/Chronith.Tests.Unit/Domain/CalendarSlotGenerationTests.cs`
- `tests/Chronith.Tests.Unit/Domain/SlotResolveTests.cs`
- `tests/Chronith.Tests.Unit/Domain/TimezoneEdgeCaseTests.cs`
- `tests/Chronith.Tests.Unit/Domain/TenantTimeZoneTests.cs`
- `tests/Chronith.Tests.Unit/Helpers/BookingTypeBuilder.cs`
- `tests/Chronith.Tests.Unit/Helpers/BookingBuilder.cs`

**Test categories:**

### BookingStateMachineTests

```csharp
// PendingPayment → Pay → PendingVerification
[Fact] void Pay_From_PendingPayment_Transitions_To_PendingVerification()
[Fact] void Pay_From_PendingVerification_Throws_InvalidStateTransition()
[Fact] void Pay_From_Confirmed_Throws_InvalidStateTransition()
[Fact] void Pay_From_Cancelled_Throws_InvalidStateTransition()

// PendingVerification → Confirm → Confirmed
[Fact] void Confirm_From_PendingVerification_Transitions_To_Confirmed()
[Fact] void Confirm_From_PendingPayment_Throws_InvalidStateTransition()
[Fact] void Confirm_From_Confirmed_Throws_InvalidStateTransition()
[Fact] void Confirm_From_Cancelled_Throws_InvalidStateTransition()

// Cancel from all non-terminal states
[Fact] void Cancel_From_PendingPayment_Transitions_To_Cancelled()
[Fact] void Cancel_From_PendingVerification_Transitions_To_Cancelled()
[Fact] void Cancel_From_Confirmed_Transitions_To_Cancelled()
[Fact] void Cancel_From_Cancelled_Throws_InvalidStateTransition()

// Audit log
[Fact] void Pay_Appends_StatusChange_With_Correct_Fields()
[Fact] void StatusChanges_Are_Append_Only()
```

### TimeSlotConflictRangeTests

```csharp
// GetEffectiveRange with buffers
[Fact] void GetEffectiveRange_WithNoBuffers_ReturnsSameBounds()
[Fact] void GetEffectiveRange_WithBufferBefore_ExpandsStart()
[Fact] void GetEffectiveRange_WithBufferAfter_ExpandsEnd()
[Fact] void GetEffectiveRange_WithBothBuffers_ExpandsBothEnds()

// Overlap detection (using effective ranges)
[Theory] // parametrized with overlap/no-overlap pairs
void Overlap_Detected_When_EffectiveRangesIntersect(...)
void No_Overlap_When_EffectiveRangesAreAdjacent(...)
void No_Overlap_When_BufferFillsGap(...)
```

### TimeSlotGenerationTests

```csharp
// Slot generation from weekly windows
[Fact] void GenerateSlots_ProducesCorrectSlots_ForSingleWindow()
[Fact] void GenerateSlots_AdvancesBy_Duration_Plus_BufferBefore_Plus_BufferAfter()
[Fact] void GenerateSlots_ExcludesSlotsThatExceedWindowEnd()
[Fact] void GenerateSlots_AcrossMultipleDays_ReturnsAllDays()
[Fact] void GenerateSlots_SkipsDaysNotInWindow()
[Fact] void GenerateSlots_RespectsFromAndToBoundary()
```

### CalendarSlotGenerationTests

```csharp
[Fact] void GenerateSlots_ReturnsOneDayPerAvailableDay()
[Fact] void GenerateSlots_SkipsUnavailableDaysOfWeek()
[Fact] void GenerateSlots_ProducesFullDayBoundsInUtc()
[Fact] void GenerateSlots_RespectsFromAndToBoundary()
```

### TimezoneEdgeCaseTests

```csharp
// DST spring forward (America/New_York: 2026-03-08 02:00 → 03:00)
[Fact] void ToUtc_DuringSpringForward_Gap_HandledCorrectly()
// DST fall back (America/New_York: 2026-11-01 02:00 → 01:00)
[Fact] void ToUtc_DuringFallBack_Ambiguous_UsesCorrectOffset()
// Window that straddles midnight in local time
[Fact] void GenerateSlots_WindowCrossingMidnight_IsHandled()
// UTC+/- offset tenants
[Fact] void ToUtc_WithPositiveOffset_IsCorrect()
[Fact] void ToUtc_WithNegativeOffset_IsCorrect()
// Calendar day boundaries in non-UTC timezone
[Fact] void CalendarSlot_DayBoundaries_CorrectInTenantTimezone()
```

**Step 1:** Write ALL failing tests first.

**Step 2:** Run `dotnet test tests/Chronith.Tests.Unit` — should see failures.

**Step 3:** Fix any domain implementation gaps found by tests.

**Step 4:** Run `dotnet test tests/Chronith.Tests.Unit` — all green.

**Step 5:** Commit.

```bash
git add .
git commit -m "test(unit): full domain unit test suite — state machine, conflict, slots, timezone"
```

---

## Task 4: Application Layer — CQRS, Validators, Pipeline Behaviors

**Goal:** All MediatR commands/queries, FluentValidation validators, pipeline behaviors, DTOs, and service interfaces.

**Files to create:**

- `src/Chronith.Application/Interfaces/ITenantContext.cs`
- `src/Chronith.Application/Interfaces/ISlotGeneratorService.cs`
- `src/Chronith.Application/Behaviors/LoggingBehavior.cs`
- `src/Chronith.Application/Behaviors/ValidationBehavior.cs`
- `src/Chronith.Application/Behaviors/PerformanceBehavior.cs`
- `src/Chronith.Application/DTOs/` (one file per DTO)
- `src/Chronith.Application/Commands/Bookings/CreateBookingCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/Bookings/PayBookingCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/Bookings/ConfirmBookingCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/Bookings/CancelBookingCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/BookingTypes/CreateBookingTypeCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/BookingTypes/UpdateBookingTypeCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/BookingTypes/DeleteBookingTypeCommand.cs` + Handler
- `src/Chronith.Application/Commands/Webhooks/CreateWebhookCommand.cs` + Handler + Validator
- `src/Chronith.Application/Commands/Webhooks/DeleteWebhookCommand.cs` + Handler
- `src/Chronith.Application/Queries/Bookings/GetBookingQuery.cs` + Handler
- `src/Chronith.Application/Queries/Bookings/ListBookingsQuery.cs` + Handler
- `src/Chronith.Application/Queries/BookingTypes/GetBookingTypeQuery.cs` + Handler
- `src/Chronith.Application/Queries/BookingTypes/ListBookingTypesQuery.cs` + Handler
- `src/Chronith.Application/Queries/Availability/GetAvailabilityQuery.cs` + Handler
- `src/Chronith.Application/Queries/Webhooks/GetWebhooksQuery.cs` + Handler
- `src/Chronith.Application/Queries/Tenant/GetTenantQuery.cs` + Handler
- `src/Chronith.Application/Notifications/BookingStatusChangedNotification.cs`
- `src/Chronith.Application/Notifications/WebhookDeliveryHandler.cs` (stub)
- `src/Chronith.Application/DependencyInjection.cs`

**ITenantContext:**

```csharp
public interface ITenantContext
{
    Guid TenantId { get; }
    string UserId { get; }
    string Role { get; }
}
```

**Pipeline Behaviors:**

```csharp
// ValidationBehavior — throws ValidationException if any validator fails
public class ValidationBehavior<TRequest, TResponse> : IPipelineBehavior<TRequest, TResponse>
    where TRequest : notnull
{
    private readonly IEnumerable<IValidator<TRequest>> _validators;
    // ... standard FluentValidation pipeline behavior
}

// PerformanceBehavior — logs warning if handler exceeds threshold
// Read threshold: 50ms, Write threshold: 75ms
// Distinguish by checking if TRequest implements IQuery marker interface
```

**ISlotGeneratorService:**

```csharp
public interface ISlotGeneratorService
{
    IReadOnlyList<(DateTimeOffset Start, DateTimeOffset End)> GenerateAvailableSlots(
        BookingType bookingType,
        TenantTimeZone tz,
        DateTimeOffset from,
        DateTimeOffset to,
        IReadOnlyList<(DateTimeOffset Start, DateTimeOffset End)> bookedSlots);
}
```

**GetAvailabilityQuery handler — key pattern:**

```csharp
// 1. Load BookingType (AsNoTracking, projected — Id, Kind, DurationMinutes, BufferBefore, BufferAfter, Windows)
// 2. Load booked slots in range (AsNoTracking, projected to Start+End only)
//    WHERE BookingTypeId = x AND Start < to AND End > from AND Status IN (...)
// 3. Call ISlotGeneratorService.GenerateAvailableSlots(...)
// 4. Return AvailabilityDto
```

**CreateBookingCommand handler — key pattern:**

```csharp
// 1. Load BookingType (AsNoTracking)
// 2. Load Tenant (for TimeZoneId)
// 3. bookingType.ResolveSlot(requestedStart, tz) → (start, end)
// 4. Retry loop (max 3):
//    a. COUNT conflict query
//    b. If count >= capacity → throw SlotConflictException
//    c. Insert booking in transaction
//    d. On DbUpdateConcurrencyException → retry
// 5. Publish BookingStatusChangedNotification
// 6. Return BookingDto
```

**DependencyInjection.cs:**

```csharp
public static IServiceCollection AddApplication(this IServiceCollection services)
{
    services.AddMediatR(cfg =>
    {
        cfg.RegisterServicesFromAssemblyContaining<IApplicationMarker>();
        cfg.AddOpenBehavior(typeof(LoggingBehavior<,>));
        cfg.AddOpenBehavior(typeof(ValidationBehavior<,>));
        cfg.AddOpenBehavior(typeof(PerformanceBehavior<,>));
    });
    services.AddValidatorsFromAssemblyContaining<IApplicationMarker>();
    services.AddScoped<ISlotGeneratorService, SlotGeneratorService>();
    return services;
}
```

**Step 1:** Write failing unit tests for `SlotGeneratorService` (the only pure-logic piece in Application).

**Step 2:** Implement `SlotGeneratorService` to pass those tests.

**Step 3:** Implement all commands, queries, behaviors, DTOs.

**Step 4:** Run `dotnet build` — 0 errors.

**Step 5:** Run `dotnet test tests/Chronith.Tests.Unit` — all green.

**Step 6:** Commit.

```bash
git add .
git commit -m "feat(application): CQRS commands/queries, validators, pipeline behaviors, slot generator"
```

---

## Task 5: Infrastructure Layer — EF Core, DbContext, Migrations

**Goal:** EF Core DbContext with all entity configurations, global query filters, indexes, optimistic concurrency, provider abstraction, and initial migration.

**Files to create:**

- `src/Chronith.Infrastructure/Persistence/ChronithDbContext.cs`
- `src/Chronith.Infrastructure/Persistence/Entities/BookingTypeEntity.cs`
- `src/Chronith.Infrastructure/Persistence/Entities/AvailabilityWindowEntity.cs`
- `src/Chronith.Infrastructure/Persistence/Entities/BookingEntity.cs`
- `src/Chronith.Infrastructure/Persistence/Entities/BookingStatusChangeEntity.cs`
- `src/Chronith.Infrastructure/Persistence/Entities/TenantEntity.cs`
- `src/Chronith.Infrastructure/Persistence/Entities/WebhookEntity.cs`
- `src/Chronith.Infrastructure/Persistence/Configurations/` (one file per entity)
- `src/Chronith.Infrastructure/Persistence/Mappers/BookingTypeMapper.cs`
- `src/Chronith.Infrastructure/Persistence/Mappers/BookingMapper.cs`
- `src/Chronith.Infrastructure/Persistence/Mappers/TenantMapper.cs`
- `src/Chronith.Infrastructure/Providers/IDbProviderConfigurator.cs`
- `src/Chronith.Infrastructure/Providers/PostgreSqlConfigurator.cs`
- `src/Chronith.Infrastructure/TenantContext/JwtTenantContext.cs`
- `src/Chronith.Infrastructure/DependencyInjection.cs`
- `src/Chronith.Infrastructure/Migrations/PostgreSQL/` (EF generated)

**ChronithDbContext key patterns:**

```csharp
public sealed class ChronithDbContext : DbContext
{
    private readonly ITenantContext _tenantContext;

    public ChronithDbContext(DbContextOptions<ChronithDbContext> options, ITenantContext tenantContext)
        : base(options)
    {
        _tenantContext = tenantContext;
    }

    public DbSet<BookingTypeEntity> BookingTypes => Set<BookingTypeEntity>();
    public DbSet<BookingEntity> Bookings => Set<BookingEntity>();
    public DbSet<TenantEntity> Tenants => Set<TenantEntity>();
    public DbSet<WebhookEntity> Webhooks => Set<WebhookEntity>();

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.ApplyConfigurationsFromAssembly(typeof(ChronithDbContext).Assembly);

        // Global query filters — only scoped to non-admin tables
        modelBuilder.Entity<BookingTypeEntity>()
            .HasQueryFilter(bt => !bt.IsDeleted && bt.TenantId == _tenantContext.TenantId);
        modelBuilder.Entity<BookingEntity>()
            .HasQueryFilter(b => !b.IsDeleted && b.TenantId == _tenantContext.TenantId);
        modelBuilder.Entity<WebhookEntity>()
            .HasQueryFilter(w => !w.IsDeleted && w.TenantId == _tenantContext.TenantId);
    }
}
```

**BookingTypeConfiguration (key parts):**

```csharp
builder.HasIndex(bt => new { bt.TenantId, bt.Slug }).IsUnique();
builder.HasIndex(bt => new { bt.TenantId, bt.IsDeleted });
builder.Property(bt => bt.RowVersion).IsRowVersion();
```

**BookingConfiguration (key parts):**

```csharp
builder.HasIndex(b => new { b.TenantId, b.BookingTypeId, b.Start, b.End });
builder.HasIndex(b => new { b.BookingTypeId, b.Status, b.Start, b.End });
builder.HasIndex(b => new { b.TenantId, b.IsDeleted });
builder.HasIndex(b => b.CustomerId);
builder.Property(b => b.RowVersion).IsRowVersion();
builder.OwnsMany(b => b.StatusChanges, sc => { ... });
```

**Static mappers (no reflection):**

```csharp
// BookingTypeMapper.cs
public static class BookingTypeMapper
{
    public static BookingType ToDomain(BookingTypeEntity entity, List<AvailabilityWindowEntity> windows)
        => entity.Kind switch
        {
            BookingKind.TimeSlot => MapTimeSlot(entity, windows),
            BookingKind.Calendar => MapCalendar(entity, windows),
            _ => throw new ArgumentOutOfRangeException()
        };

    private static TimeSlotBookingType MapTimeSlot(...) { ... }
    private static CalendarBookingType MapCalendar(...) { ... }
}
```

**IDbProviderConfigurator:**

```csharp
public interface IDbProviderConfigurator
{
    void Configure(DbContextOptionsBuilder options, string connectionString);
}

public sealed class PostgreSqlConfigurator : IDbProviderConfigurator
{
    public void Configure(DbContextOptionsBuilder options, string connectionString)
        => options.UseNpgsql(connectionString, npgsql =>
            npgsql.MigrationsHistoryTable("__EFMigrationsHistory", "chronith"));
}
```

**appsettings.json provider config:**

```json
{
  "Database": {
    "Provider": "PostgreSQL",
    "ConnectionString": "Host=localhost;Database=chronith;Username=chronith;Password=chronith"
  }
}
```

**DependencyInjection.cs:**

```csharp
public static IServiceCollection AddInfrastructure(
    this IServiceCollection services, IConfiguration configuration)
{
    var provider = configuration["Database:Provider"] ?? "PostgreSQL";
    var connectionString = configuration["Database:ConnectionString"]!;

    var configurator = provider switch
    {
        "PostgreSQL" => (IDbProviderConfigurator)new PostgreSqlConfigurator(),
        _ => throw new NotSupportedException($"Provider '{provider}' is not supported.")
    };

    services.AddDbContext<ChronithDbContext>((sp, options) =>
    {
        configurator.Configure(options, connectionString);
    });

    services.AddScoped<ITenantContext, JwtTenantContext>();
    return services;
}
```

**After implementation:** Generate migration:

```bash
cd src/Chronith.API
dotnet ef migrations add InitialCreate \
  --project ../Chronith.Infrastructure \
  --output-dir Migrations/PostgreSQL \
  --context ChronithDbContext
```

**Step 1:** Write failing integration tests (Task 6 drives this — write minimal tests here to verify entity configs compile and context can be created).

**Step 2:** Implement all infrastructure files above.

**Step 3:** Generate migration.

**Step 4:** Run `dotnet build` — 0 errors.

**Step 5:** Commit.

```bash
git add .
git commit -m "feat(infrastructure): EF Core DbContext, entity configs, indexes, migrations, provider abstraction"
```

---

## Task 6: Integration Tests — EF Core + Testcontainers

**Goal:** Integration tests against a real Postgres instance. Verify queries hit indexes, migrations apply cleanly, conflict check runs as SQL, transactions are safe.

**Files to create:**

- `tests/Chronith.Tests.Integration/Fixtures/PostgresFixture.cs`
- `tests/Chronith.Tests.Integration/Fixtures/DbContextFactory.cs`
- `tests/Chronith.Tests.Integration/Persistence/MigrationTests.cs`
- `tests/Chronith.Tests.Integration/Persistence/BookingConflictQueryTests.cs`
- `tests/Chronith.Tests.Integration/Persistence/AvailabilityQueryTests.cs`
- `tests/Chronith.Tests.Integration/Persistence/BookingTypeSlugTests.cs`
- `tests/Chronith.Tests.Integration/Persistence/SoftDeleteFilterTests.cs`
- `tests/Chronith.Tests.Integration/Persistence/OptimisticConcurrencyTests.cs`
- `tests/Chronith.Tests.Integration/Persistence/PaginationTests.cs`
- `tests/Chronith.Tests.Integration/Helpers/SeedData.cs`

**PostgresFixture (shared across all integration tests via CollectionDefinition):**

```csharp
public sealed class PostgresFixture : IAsyncLifetime
{
    public PostgreSqlContainer Container { get; } = new PostgreSqlBuilder()
        .WithImage("postgres:17-alpine")
        .WithDatabase("chronith_test")
        .WithUsername("test")
        .WithPassword("test")
        .Build();

    public Task InitializeAsync() => Container.StartAsync();
    public Task DisposeAsync() => Container.DisposeAsync().AsTask();
}

[CollectionDefinition("Integration")]
public class IntegrationCollection : ICollectionFixture<PostgresFixture> { }
```

**Test coverage:**

```csharp
// MigrationTests
[Fact] async Task Migration_AppliesCleanly_ToFreshDatabase()
[Fact] async Task Migration_IsIdempotent_WhenAppliedTwice()

// BookingConflictQueryTests
[Fact] async Task ConflictQuery_ReturnsZero_WhenNoOverlappingBookings()
[Fact] async Task ConflictQuery_ReturnsCount_WhenOverlapExists()
[Fact] async Task ConflictQuery_IgnoresCancelledBookings()
[Fact] async Task ConflictQuery_IncludesPendingPaymentBookings()
[Fact] async Task ConflictQuery_IncludesPendingVerificationBookings()
[Fact] async Task ConflictQuery_RespectsBufferAdjustedRange()
[Theory] async Task ConflictQuery_CorrectlyHandles10kBookings_WithIndex(int bookingCount)

// AvailabilityQueryTests
[Fact] async Task AvailabilityQuery_ProjectsOnlyStartAndEnd_NoFullEntity()
[Fact] async Task AvailabilityQuery_FiltersByTenantId()
[Fact] async Task AvailabilityQuery_FiltersByBookingTypeId()
[Fact] async Task AvailabilityQuery_OnlyReturnsConflictingStatuses()

// BookingTypeSlugTests
[Fact] async Task SlugLookup_HitsCompositeIndex_ForTenantAndSlug()
[Fact] async Task SlugLookup_IsCaseInsensitive() // verify collation

// SoftDeleteFilterTests
[Fact] async Task DeletedBookings_NotReturnedByDefaultQuery()
[Fact] async Task IgnoreQueryFilters_ReturnsDeletedRecords()
[Fact] async Task DeletedBookingTypes_NotReturnedByDefaultQuery()

// OptimisticConcurrencyTests
[Fact] async Task ConcurrentInsert_WithRowVersionConflict_ThrowsDbUpdateConcurrencyException()
[Fact] async Task ConcurrentInsert_RetryLogic_EventuallySucceeds()

// PaginationTests
[Fact] async Task ListBookings_ReturnsPaginatedResults_InCorrectOrder()
[Fact] async Task ListBookings_RespectsPageSize()
```

**Step 1:** Write ALL failing tests.

**Step 2:** Run tests — should fail (no infrastructure yet or wiring issues found).

**Step 3:** Fix any gaps found in Task 5 infrastructure.

**Step 4:** Run `dotnet test tests/Chronith.Tests.Integration` — all green.

**Step 5:** Commit.

```bash
git add .
git commit -m "test(integration): EF queries, indexes, migrations, conflict check, soft delete, concurrency"
```

---

## Task 7: API Layer — FastEndpoints, JWT, Exception Handling

**Goal:** All FastEndpoints endpoints, JWT auth configuration, global exception handler, health checks, tenant resolution from JWT, Program.cs wiring.

**Files to create:**

- `src/Chronith.API/Program.cs`
- `src/Chronith.API/Endpoints/Tenant/GetTenantEndpoint.cs`
- `src/Chronith.API/Endpoints/BookingTypes/CreateBookingTypeEndpoint.cs`
- `src/Chronith.API/Endpoints/BookingTypes/ListBookingTypesEndpoint.cs`
- `src/Chronith.API/Endpoints/BookingTypes/GetBookingTypeEndpoint.cs`
- `src/Chronith.API/Endpoints/BookingTypes/UpdateBookingTypeEndpoint.cs`
- `src/Chronith.API/Endpoints/BookingTypes/DeleteBookingTypeEndpoint.cs`
- `src/Chronith.API/Endpoints/Availability/GetAvailabilityEndpoint.cs`
- `src/Chronith.API/Endpoints/Bookings/CreateBookingEndpoint.cs`
- `src/Chronith.API/Endpoints/Bookings/ListBookingsEndpoint.cs`
- `src/Chronith.API/Endpoints/Bookings/GetBookingEndpoint.cs`
- `src/Chronith.API/Endpoints/Bookings/PayBookingEndpoint.cs`
- `src/Chronith.API/Endpoints/Bookings/ConfirmBookingEndpoint.cs`
- `src/Chronith.API/Endpoints/Bookings/CancelBookingEndpoint.cs`
- `src/Chronith.API/Endpoints/Webhooks/CreateWebhookEndpoint.cs`
- `src/Chronith.API/Endpoints/Webhooks/ListWebhooksEndpoint.cs`
- `src/Chronith.API/Endpoints/Webhooks/DeleteWebhookEndpoint.cs`
- `src/Chronith.API/Endpoints/Payments/PaymentWebhookEndpoint.cs`
- `src/Chronith.API/Middleware/ExceptionHandlingMiddleware.cs`
- `src/Chronith.API/HealthChecks/DatabaseHealthCheck.cs`
- `src/Chronith.API/appsettings.json`
- `src/Chronith.API/appsettings.Development.json`

**Program.cs key pattern:**

```csharp
var builder = WebApplication.CreateBuilder(args);

builder.Services
    .AddApplication()
    .AddInfrastructure(builder.Configuration)
    .AddAuthenticationJwtBearer(s =>
    {
        s.SigningKey = builder.Configuration["Jwt:SigningKey"]!;
    })
    .AddAuthorization()
    .AddFastEndpoints()
    .AddHealthChecks()
        .AddCheck<DatabaseHealthCheck>("database");

builder.Host.UseSerilog(...);

var app = builder.Build();

app.UseExceptionHandler(ExceptionHandlingMiddleware.Configure);
app.UseAuthentication()
   .UseAuthorization()
   .UseFastEndpoints(c =>
   {
       c.Errors.UseProblemDetails();
   });

app.MapHealthChecks("/health/live");
app.MapHealthChecks("/health/ready", new HealthCheckOptions
{
    Predicate = check => check.Name == "database"
});

app.Run();
```

**Endpoint pattern (one example):**

```csharp
public class CreateBookingEndpoint : Endpoint<CreateBookingRequest, CreateBookingResponse>
{
    private readonly IMediator _mediator;

    public CreateBookingEndpoint(IMediator mediator) => _mediator = mediator;

    public override void Configure()
    {
        Post("/booking-types/{slug}/bookings");
        Roles("TenantAdmin", "TenantStaff", "Customer");
    }

    public override async Task HandleAsync(CreateBookingRequest req, CancellationToken ct)
    {
        var result = await _mediator.Send(new CreateBookingCommand
        {
            BookingTypeSlug = Route<string>("slug")!,
            StartTime = req.StartTime,
            CustomerEmail = req.CustomerEmail
        }, ct);

        await SendCreatedAtAsync<GetBookingEndpoint>(
            new { bookingId = result.Id }, result, cancellation: ct);
    }
}
```

**ExceptionHandlingMiddleware maps:**

- `ValidationException` → 400 ProblemDetails with errors
- `NotFoundException` → 404
- `SlotConflictException` → 409
- `SlotNotInWindowException` → 422
- `InvalidStateTransitionException` → 422
- `DomainException` → 422
- Unhandled → 500

**Step 1:** Write minimal functional test (Task 8 has full suite — just verify `GET /health/live` returns 200 here as a smoke test).

**Step 2:** Implement all files above.

**Step 3:** Run `dotnet build` — 0 errors.

**Step 4:** Run smoke test — passes.

**Step 5:** Commit.

```bash
git add .
git commit -m "feat(api): FastEndpoints endpoints, JWT auth, exception handling, health checks"
```

---

## Task 8: Functional Tests — Full HTTP Stack

**Goal:** End-to-end tests over the real HTTP stack using `WebApplicationFactory` and Testcontainers Postgres.

**Files to create:**

- `tests/Chronith.Tests.Functional/Fixtures/FunctionalTestFixture.cs`
- `tests/Chronith.Tests.Functional/Fixtures/TestJwtFactory.cs`
- `tests/Chronith.Tests.Functional/BookingTypes/CreateBookingTypeTests.cs`
- `tests/Chronith.Tests.Functional/BookingTypes/UpdateBookingTypeTests.cs`
- `tests/Chronith.Tests.Functional/BookingTypes/DeleteBookingTypeTests.cs`
- `tests/Chronith.Tests.Functional/Availability/GetAvailabilityTests.cs`
- `tests/Chronith.Tests.Functional/Bookings/CreateBookingTests.cs`
- `tests/Chronith.Tests.Functional/Bookings/BookingLifecycleTests.cs`
- `tests/Chronith.Tests.Functional/Bookings/BookingAuthorizationTests.cs`
- `tests/Chronith.Tests.Functional/Bookings/ListBookingsTests.cs`
- `tests/Chronith.Tests.Functional/Webhooks/WebhookCrudTests.cs`
- `tests/Chronith.Tests.Functional/Tenant/GetTenantTests.cs`

**FunctionalTestFixture — key pattern:**

```csharp
public sealed class FunctionalTestFixture : WebApplicationFactory<Program>, IAsyncLifetime
{
    private readonly PostgreSqlContainer _postgres = new PostgreSqlBuilder()
        .WithImage("postgres:17-alpine")
        .Build();

    public async Task InitializeAsync()
    {
        await _postgres.StartAsync();
        // Seed tenant + apply migrations
    }

    protected override void ConfigureWebHost(IWebHostBuilder builder)
    {
        builder.ConfigureServices(services =>
        {
            // Replace DbContext with test connection string
            var descriptor = services.Single(d => d.ServiceType == typeof(DbContextOptions<ChronithDbContext>));
            services.Remove(descriptor);
            services.AddDbContext<ChronithDbContext>(...); // use _postgres.GetConnectionString()
        });
    }
}
```

**TestJwtFactory — issues JWTs for each role:**

```csharp
public static class TestJwtFactory
{
    public static string ForAdmin(Guid tenantId, string userId = "admin-1")
        => JwtBearer.CreateToken(o =>
        {
            o.SigningKey = TestConstants.JwtSigningKey;
            o.User.Claims.Add(("tenant_id", tenantId.ToString()));
            o.User.Claims.Add(("sub", userId));
            o.User.Roles.Add("TenantAdmin");
        });

    public static string ForStaff(Guid tenantId, string userId = "staff-1") => ...;
    public static string ForCustomer(Guid tenantId, string customerId) => ...;
}
```

**Test coverage:**

```csharp
// BookingLifecycleTests — full flow
[Fact] async Task FullFlow_TimeSlot_CreatePayConfirm_Succeeds()
[Fact] async Task FullFlow_Calendar_CreatePayConfirm_Succeeds()
[Fact] async Task FullFlow_Create_Cancel_Slot_Becomes_Available_Again()

// Conflict
[Fact] async Task CreateBooking_WhenSlotTaken_Returns409()
[Fact] async Task CreateBooking_WhenCapacityNotReached_Succeeds()

// Authorization
[Fact] async Task Customer_CannotCallPay_Returns403()
[Fact] async Task Customer_CanCancelOwnBooking_Returns200()
[Fact] async Task Customer_CannotCancelOtherCustomersBooking_Returns403()
[Fact] async Task Staff_CannotCreateBookingType_Returns403()
[Fact] async Task Unauthenticated_Returns401()

// Availability
[Fact] async Task GetAvailability_ReturnsSlots_ForTimeSlotType()
[Fact] async Task GetAvailability_ReturnsDays_ForCalendarType()
[Fact] async Task GetAvailability_ExcludesBookedSlots()

// Pagination
[Fact] async Task ListBookings_ReturnsPaginatedResults()
[Fact] async Task ListBookings_RespectsPageAndPageSize()
```

**Step 1:** Write ALL failing tests.

**Step 2:** Fix any wiring issues found in API layer.

**Step 3:** Run `dotnet test tests/Chronith.Tests.Functional` — all green.

**Step 4:** Commit.

```bash
git add .
git commit -m "test(functional): full HTTP booking flows, auth matrix, availability, pagination"
```

---

## Task 9: Performance Tests (BenchmarkDotNet)

**Goal:** Micro-benchmarks for slot generation and conflict algorithms. These run against in-memory data — no DB.

**Files to create:**

- `tests/Chronith.Tests.Performance/Benchmarks/SlotGenerationBenchmarks.cs`
- `tests/Chronith.Tests.Performance/Benchmarks/ConflictCheckBenchmarks.cs`
- `tests/Chronith.Tests.Performance/Program.cs`

```csharp
// SlotGenerationBenchmarks
[Benchmark] public void GenerateSlots_TimeSlot_1WeekWindow_NoExistingBookings()
[Benchmark] public void GenerateSlots_TimeSlot_1WeekWindow_With500BookedSlots()
[Benchmark] public void GenerateSlots_Calendar_30DayWindow_NoExistingBookings()
[Benchmark] public void GenerateSlots_Calendar_90DayWindow_5000BookedDays()

// ConflictCheckBenchmarks — algorithm only, mocked data
[Benchmark] public void ConflictCheck_10kBookings_OverlapFound()
[Benchmark] public void ConflictCheck_10kBookings_NoOverlap()
```

**Step 1:** Implement benchmarks.

**Step 2:** Run `dotnet run -c Release` in `Chronith.Tests.Performance` — verify benchmarks execute and produce results.

**Step 3:** Commit.

```bash
git add .
git commit -m "test(performance): BenchmarkDotNet slot generation and conflict algorithm benchmarks"
```

---

## Task 10: Load Tests (k6)

**Goal:** k6 scripts that target the running Docker stack. Test concurrent booking, double-booking prevention, and measure latency against targets.

**Files to create:**

- `tests/Chronith.Tests.Load/scripts/availability.js`
- `tests/Chronith.Tests.Load/scripts/create-booking.js`
- `tests/Chronith.Tests.Load/scripts/concurrent-booking.js`
- `tests/Chronith.Tests.Load/scripts/booking-lifecycle.js`
- `tests/Chronith.Tests.Load/README.md`

**concurrent-booking.js — key scenario:**

```javascript
// 50 VUs simultaneously attempt to book the same slot
// Expected: exactly capacity bookings succeed (201), rest get 409
// Validates: no double-booking under race conditions

export let options = {
  vus: 50,
  duration: "30s",
  thresholds: {
    http_req_duration: ["p(95)<80"], // 95th percentile < 80ms
    http_req_failed: ["rate<0.01"],
  },
};
```

**availability.js — latency target:**

```javascript
// 100 VUs hitting GET /booking-types/{slug}/availability
// Threshold: p(95) < 100ms
export let options = {
  vus: 100,
  duration: "60s",
  thresholds: {
    http_req_duration: ["p(95)<100"],
  },
};
```

**Step 1:** Write all k6 scripts.

**Step 2:** Document run instructions in `README.md`.

**Step 3:** Commit.

```bash
git add .
git commit -m "test(load): k6 scripts for concurrent booking, availability latency, double-booking prevention"
```

---

## Task 11: Containerization + TEST_PLAN.md

**Goal:** Multi-stage Dockerfile, docker-compose with Postgres, health checks wired, and TEST_PLAN.md.

**Files to create:**

- `Dockerfile`
- `docker-compose.yml`
- `docker-compose.override.yml` (dev overrides)
- `.dockerignore`
- `TEST_PLAN.md`

**Dockerfile:**

```dockerfile
# Stage 1: Build
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src
COPY ["src/Chronith.API/Chronith.API.csproj", "src/Chronith.API/"]
COPY ["src/Chronith.Application/Chronith.Application.csproj", "src/Chronith.Application/"]
COPY ["src/Chronith.Infrastructure/Chronith.Infrastructure.csproj", "src/Chronith.Infrastructure/"]
COPY ["src/Chronith.Domain/Chronith.Domain.csproj", "src/Chronith.Domain/"]
RUN dotnet restore "src/Chronith.API/Chronith.API.csproj"
COPY . .
RUN dotnet publish "src/Chronith.API/Chronith.API.csproj" -c Release -o /app/publish

# Stage 2: Runtime
FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS runtime
WORKDIR /app
RUN adduser --disabled-password --gecos "" appuser && chown -R appuser /app
USER appuser
COPY --from=build /app/publish .
EXPOSE 8080
HEALTHCHECK --interval=10s --timeout=5s --start-period=30s \
  CMD curl -f http://localhost:8080/health/live || exit 1
ENTRYPOINT ["dotnet", "Chronith.API.dll"]
```

**docker-compose.yml:**

```yaml
version: "3.9"
services:
  chronith-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Database__Provider=PostgreSQL
      - Database__ConnectionString=Host=postgres;Database=chronith;Username=chronith;Password=chronith
      - Jwt__SigningKey=${JWT_SIGNING_KEY}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: chronith
      POSTGRES_USER: chronith
      POSTGRES_PASSWORD: chronith
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chronith"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
```

**TEST_PLAN.md** — see design doc Section 14 for full content. Must include:

- Scope
- Architecture overview
- Test matrix (all 5 layers)
- Performance benchmarks with targets
- Load strategy (k6 scenarios)
- Concurrency scenarios
- Risk analysis
- Timezone edge cases
- Conflict edge cases
- How to run each test layer

**Step 1:** Write Dockerfile, docker-compose files, .dockerignore.

**Step 2:** Run `docker compose build` — must succeed.

**Step 3:** Run `docker compose up` — API must reach healthy state.

**Step 4:** Curl `http://localhost:8080/health/ready` — must return 200.

**Step 5:** Write TEST_PLAN.md.

**Step 6:** Run full test suite: `dotnet test Chronith.sln` — all green.

**Step 7:** Final commit.

```bash
git add .
git commit -m "feat(containers): multi-stage Dockerfile, docker-compose, health checks, TEST_PLAN.md"
```

---

## Definition of Done

- [ ] `dotnet build Chronith.sln` — 0 errors, 0 warnings
- [ ] `dotnet test Chronith.sln` — all tests green
- [ ] `docker compose build && docker compose up` — API reaches healthy state
- [ ] `GET /health/ready` returns 200
- [ ] All 15 endpoints respond to authenticated requests
- [ ] Concurrent booking test (50 VUs, same slot) produces no double bookings
- [ ] BenchmarkDotNet slot generation completes within performance targets
- [ ] TEST_PLAN.md exists and is accurate
- [ ] `.gitignore` present and correct
- [ ] No provider-specific SQL anywhere in the codebase
- [ ] No AutoMapper, no Mapster, no reflection-based mappers
- [ ] All reads use `AsNoTracking`
